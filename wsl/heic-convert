#!/bin/bash

# find /mnt/p/giuly/import -name "*.HEIC" -exec ./heic-convert {} \;

file="$1"
if [[ -f $file && $file != *"recycle"* && $file != "/mnt/p/heic/"* ]]; then
	echo $file
	heif-convert $file ${file/%.HEIC/.png}
	mv $file /mnt/p/HEIC
fi
